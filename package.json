{
  "name": "hadas-scheduling",
  "version": "0.35.0",
  "private": true,
  "description": "מערכת ניהול שיבוצים מעון הדס — שיבוצים, צוות, בקשות, הודעות ולוח שנה",
  "license": "UNLICENSED",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node scripts/check.js",
    "qa": "npm test && npm run check"
  }
}
