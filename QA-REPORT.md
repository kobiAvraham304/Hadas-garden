# דוח בדיקות — מערכת ניהול שיבוצים מעון הדס 0.10.0

## תוצאה

**69 מתוך 69 בדיקות אוטומטיות עברו בהצלחה.**

בנוסף בוצעה בדיקת Chromium עם נתוני שרת מדומים עבור סייעת וגננת:

| תצוגה | רוחב מסך | גלישה אופקית | הרשאת השיבוץ שנבדקה | שגיאות JavaScript |
|---|---:|---:|---|---:|
| סייעת — טלפון קטן | 360px | 0 | השיבוץ שלי בלבד | 0 |
| סייעת — טלפון | 390px | 0 | השיבוץ שלי בלבד | 0 |
| גננת — טלפון | 390px | 0 | שבוע/יום/אישי | 0 |
| גננת — מחשב | 1440px | 0 | שבוע/יום/אישי | 0 |

חלון הבקשה ומרכז העדכונים נשארו בתוך גבולות המסך בכל התצוגות שנבדקו.

## הרשאות ופרטיות

- נבדקה הרשאת צפייה מלאה לגננת, אחות, מנהלת מעון ומזכירה.
- נבדקה תצוגה אישית בלבד לסייעת ולסייע.
- נבדק שהסינון מתבצע בשרת ולא רק באמצעות הסתרת כפתורים בדפדפן.
- נבדק שמטען נתונים של עובד רגיל אינו כולל פרטי העסקה וניהול פרטיים.
- נבדקו סשן, CSRF, נעילת התחברות והצפנת סיסמאות `scrypt`.

## החלפות

- נבדק שטופס החלפה מבקש תאריך ועובד מחליף בלבד.
- נבדק שמוצגים רק עובדים ביום חופשי, ללא שיבוץ ובעלי חשבון פעיל.
- נבדק שעובד בחופשה או במחלה אינו מוצע כמחליף.
- נבדק שלמבקש חייב להיות שיבוץ ביום שנבחר.
- נבדקו אישור ודחייה על ידי העובד שנבחר.
- נבדק שאישור הנהלה חסום עד להסכמת העובד.
- נבדקה הזרמה אטומית של כל מקטעי השיבוץ באותו יום.

## חופשה, מחלה וקבצים

- נבדקו תאריך התחלה ותאריך סיום בחופשה ובמחלה.
- נבדקה מניעת תאריך סיום הקודם לתאריך ההתחלה.
- נבדקו סוגי הקבצים המותרים ומגבלת 3MB.
- נבדק שהדלי של אישורי המחלה פרטי.
- נבדק שקישור לאישור ניתן רק למגיש הבקשה או למנהל.
- נבדק שקובץ שהועלה מנוקה במקרה ששמירת הבקשה נכשלת.

## עדכונים

- נבדקו מונה שלא נקראו, פתיחת מרכז העדכונים וסימון כנקרא.
- נבדקו עדכונים אישיים למבקש, לעובד שנבחר ולהנהלה לפי שלב ההחלפה.
- נבדקה גישה למרכז העדכונים מכותרת המערכת ומהמסך הראשי.
- נבדקה לחיצה על עדכון שפותחת את המסך הרלוונטי.

## מסד נתונים, קוד ופריסה

- נבדק שהעדכון `update-v0.10.0.sql` אינו מכיל `DROP TABLE` ואינו מוחק נתונים קיימים.
- נבדקו הטבלה `hadas_notifications`, שדות טווח התאריכים ודלי Storage הפרטי.
- נבדקו RLS והרשאות `service_role` לאובייקטים החדשים.
- נבדקה פונקציית הזרמת הבקשות המעודכנת.
- כל קובצי JavaScript עברו בדיקת תחביר.
- `npm run check` לא מצא Supabase Secret Key בקבצים.
- נשארה פונקציית Vercel אחת בלבד, מתחת למגבלת תוכנית Hobby.
- האפשרות `אחר` אינה קיימת בטופס ואינה מתקבלת ב־API.

צילומי הבדיקה והמדדים נמצאים בתיקיית `qa`. בדיקות הדפדפן בוצעו עם נתוני שרת מדומים; לאחר ההעלאה יש לוודא `Ready` ב־Vercel ומסך `/health` ירוק.
