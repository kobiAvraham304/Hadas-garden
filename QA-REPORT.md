# דוח בדיקות — גרסה 0.16.0

## תוצאה

- **121 מתוך 121 בדיקות אוטומטיות עברו.**
- **37 קובצי JavaScript** עברו `node --check` ללא שגיאות תחביר.
- סריקת האבטחה עברה ולא נמצא Supabase Secret Key בקבצים.
- קיימת פונקציית Vercel אחת בלבד: `api/index.js`.
- גרסת האתר: `0.16.0`; גרסת המסד: `0.12.0`.

## בדיקות חדשות למנוע ההתאמה

- עובד פנוי מופיע במועמדים.
- העברה מכיתה אחרת מופיעה רק כאשר כיתת המקור נשארת עם התקן וההובלה הנדרשים.
- עובד בחופשה או מחלה ואילוץ `אסור לשבץ` אינם מופיעים.
- ציוני ההתאמה נשארים בין 1 ל־100.
- אותו עובד מקבל אותו ציון ואותן סיבות בהוספה ובהחלפה כאשר ההקשר זהה.
- העברה נחסמת כאשר כיתת המקור תרד מתחת לתקן.
- פעולה אחת בשרת מטפלת בהמלצה ישירה ובהעברה בטוחה.
- כל הנתונים נבדקים מחדש בעת השמירה ולא מסתמכים רק על התצוגה בדפדפן.

## בדיקות ממשק וקוד

- נוספו כללי containment לרוחב העמוד, הכותרת, תצוגת השבוע, הכרטיסים והחלוניות.
- נבדקו כללי 360px ו־390px עבור ניווט, פעולות שיבוץ, בחירת עובד וחלוניות.
- נבדק שכפתור הסגירה נמצא בכותרת דביקה ואינו נגלל עם תוכן החלונית.
- נבדק שכל שדות התאריך והשעה מוגבלים לרוחב החלונית.
- נבדק שמטמון ההתאמה משותף להוספה ולהחלפה ומתאפס לאחר שינוי שיבוץ.
- בדיקות החוזה, האבטחה, הנתיבים, התקינה, ההרשאות והשיבוץ האוטומטי הקיימות ממשיכות לעבור.

בדיקת המראה הסופית ב־Safari של המכשיר מתבצעת לאחר הפריסה, משום שמנוע התצוגה של iOS אינו זמין בסביבת הבדיקה המקומית.
