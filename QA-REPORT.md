# דוח בדיקות — גרסה 0.13.0

## תוצאה

- **105 מתוך 105 בדיקות אוטומטיות עברו.**
- כל קובצי JavaScript עברו בדיקת תחביר.
- סריקת האבטחה עברה ולא נמצא Supabase Secret Key בקובצי ההפצה.
- בתיקיית `api` קיימת פונקציית Vercel אחת בלבד.
- אין שינוי ב־SQL או בסכמת Supabase בגרסה זו.

## בדיקות פונקציונליות

נבדקו:

- הסרת „בדיקת חיבור” ממסך הכניסה.
- הנחיית איפוס הסיסמה והקרדיט לקובי אברהם.
- הצגת סמל המערכת במסכי הכניסה והמערכת.
- הסתרת שגיאות התקינה עד ללחיצה על הכפתור.
- פתיחה וסגירה של רשימת השגיאות ועדכון המונה.
- פעולות התיקון מתוך כרטיסי השגיאות.
- נרמול ציוני התאמה לסולם 1–100.
- הצגת סקלה חזותית להצעות עובדים בתפעול היומי ובשיבוצים.
- הצגת סטטוס רישום רק למנהלות.
- שמירת `last_login_at` מחוץ למידע הנשלח לעובדים רגילים.
- תפקידי יום ברורים והצעה אוטומטית לפי תפקיד העובד.
- הגדרות תקינה מוכנות, כפתורי +/− ותצוגה מקדימה.
- מטמון חישובי תקינה למניעת חישוב כפול ללא שינוי.
- התאמה בין גרסת האתר `0.13.0` לגרסת מסד הנתונים `0.12.0`.

## בדיקות ויזואליות

בוצעה בדיקת Chromium מקומית בגדלים:

- מחשב: `1440×1100`.
- טלפון: `390×844`.
- טלפון צר: `360×800`.
- מסך הכניסה נבדק גם במחשב וגם בטלפון.

בכל הגדלים שנבדקו:

- גלישה אופקית: `0`.
- רכיבים חתוכים מחוץ למסך: `0`.
- כפתורים גלויים קטנים מ־40px במחשב או מ־44px בטלפון: `0`.
- שגיאות JavaScript בעמוד הבדיקה: `0`.
- „בדיקת חיבור” במסך הכניסה: לא קיימת.
- הנחיית איפוס סיסמה, הקרדיט והסמל: מוצגים.

תוצאות המדידה נשמרו ב־`qa/v013-visual-results.json`, וצילומי הבדיקה נשמרו בתיקיית `qa`.

## מגבלות הבדיקה

הבדיקות בוצעו מקומית עם נתוני הדמיה. הגרסה לא נפרסה בפועל לחשבון Vercel ולא נבדקה מול הנתונים החיים של המעון. האישור הסופי בסביבה החיה הוא:

1. פריסת Vercel במצב `Ready`.
2. מסך `/health` ירוק עם גרסת אתר `0.13.0` וגרסת מסד `0.12.0`.
3. רענון מלא בטלפון ובדיקת כניסה של מנהלת ועובד רגיל.
