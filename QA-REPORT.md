# דוח בדיקות — גרסה 0.14.0

## תוצאות

- 109 מתוך 109 בדיקות אוטומטיות עברו.
- כל קובצי JavaScript עברו `node --check`.
- סריקת אבטחה עברה ולא נמצא Supabase Secret Key בקבצים.
- נשארה פונקציית Vercel אחת בלבד: `api/index.js`.
- גרסת האתר היא `0.14.0`; גרסת מסד הנתונים נשארה `0.12.0`.

## מיתוג ושיתוף

נבדקו:

- `favicon.ico`
- `favicon.svg`
- סמלי PNG בגודל 32, 180, 192 ו־512 פיקסלים
- `site.webmanifest`
- תמונת שיתוף `og-image.png` בגודל 1200×630
- תגיות Open Graph ו־Twitter Card עם כתובת התמונה המלאה של האתר

## ניהול עובדים

נבדקו:

- הצגת שם הכיתה בלבד לעובד עם שיוך קבוע.
- סטטוס הרשמה קטן ואינו משתלט על הכרטיס.
- טופס מחולק לחלקים ברורים.
- ניווט מהיר בין חלקי הטופס.
- הסתרה של שיוך ושעות לעובדים שאינם חלק ממערך השיבוץ.
- חיווי `מאפס סיסמה…`, נעילת הכפתור והודעת הצלחה מפורשת.
- איפוס סיסמה והפעלה/השבתה ללא רענון מלא של כל המערכת.

## בדיקות תצוגה

נבנו מסכי בדיקה ייעודיים לכרטיסי עובדים ולטופס עובד. הם הורצו במנוע Chromium בגדלים:

- מחשב: 1440px
- טלפון: 390px
- טלפון צר: 360px

בכל התצוגות שנבדקו:

- רוחב העמוד שווה לרוחב המסך — ללא גלישה אופקית.
- לא נמצאו כפתורים קטנים מ־40px במסכים שנבדקו.
- חלון טופס העובד נשאר בתוך גבולות המסך.
- לא נרשמו שגיאות JavaScript.

תוצאות המדידה נשמרו ב־`qa/v014-visual-results.json`.
