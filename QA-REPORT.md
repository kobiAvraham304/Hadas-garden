# דוח בדיקות — מערכת ניהול שיבוצים מעון הדס 0.6.0

## תוצאה כוללת

**44 מתוך 44 בדיקות אוטומטיות עברו בהצלחה.**

בנוסף בוצעה הפעלה אמיתית של הממשק ב־Chromium עם נתוני בדיקה, ברוחב מחשב `1440px`, טלפון `390px` וטלפון צר `360px`.

## תוצאות בדיקת דפדפן

| תצוגה | גלישה אופקית של הדף | כפתורים קטנים מדי | חפיפות ניווט | שגיאות JavaScript | טבלת חופשות |
|---|---:|---:|---:|---:|---:|
| מחשב 1440px | 0 | 0 | 0 | 0 | מוצגת |
| טלפון 390px | 0 | 0 | 0 | 0 | מוצגת |
| טלפון 360px | 0 | 0 | 0 | 0 | מוצגת |

תוצאות המדידה נשמרו ב־`qa/browser-checks-v0.6.json`. צילומי המסכים נוצרו ונבדקו במהלך תהליך ה־QA, אך אינם כלולים בחבילת הפריסה כדי לא להכביד על GitHub ועל Vercel.

## מסכים שנבדקו בדפדפן

- מסך ראשי.
- שיבוצים.
- בקשות.
- נוכחות.
- הודעות.
- משימות.
- לוח שנה.
- ניהול עובדות.
- תפריט “עוד” בטלפון.
- אזור החופשות וההיעדרויות בשיבוץ.

## בדיקות עיצוב ורספונסיביות

- הניווט בטלפון מוצמד לתחתית המסך ולא מכסה את הכותרת.
- מסכים משניים נפתחים מתפריט תחתון ייעודי.
- לא נמצאו כפתורים שחופפים זה לזה.
- לא נמצאה גלילה אופקית של כל העמוד.
- כרטיסי בקשות, הודעות, משימות ועובדות עוברים לעמודה אחת בטלפון.
- אזורי פעולות עוברים לגריד מתאים למסך צר.
- שורות שמות ושעות בכרטיסי הכיתות נשארות מופרדות וקריאות.
- אירועי לוח השנה בטלפון עומדים בגודל הלחיצה שנבדק.
- חלונות וטפסים מותאמים לתצוגת גיליון תחתון בטלפון.
- שדות שעה וטקסטי שעה מוצגים בכיוון `LTR` עם ספרות מיושרות.
- נבדקה תמיכה ב־`prefers-reduced-motion`.

## בדיקות חופשות והיעדרויות

- אזור החופשות מוצג בתחתית השיבוץ.
- בתצוגת שבוע מוצגים שישה ימים.
- בתצוגת יום מוצג היום שנבחר בלבד.
- חופשה, יום חופשי ומחלה מאושרת מזוהים מתוך הנתונים הקיימים.
- יום חופשי קבוע נוסף לרשימה אוטומטית.
- כפילויות לאותה עובדת ולאותו יום מאוחדות.
- לעובדות נשלחים רק מזהה עובדת, תאריך וסוג היעדרות.
- נימוק הבקשה, הערות מנהלת ונתונים פרטיים אינם נכללים ברשימה.

## בדיקות מסננים ונגישות

- מסנני בקשות עובדים באמצעות כפתורי Chip נגישים.
- מסנני מצב וסוג עובדת עובדים באותו דפוס.
- הבחירה הפעילה מסתנכרנת עם ערך הסינון הפנימי.
- חיפוש ומסננים מסתדרים מחדש במובייל.
- כל הכפתורים כוללים מצב מיקוד ברור.
- נשמרה תמיכה בהקלדה, מגע ולחיצה.

## בדיקות קוד ותשתית

- כל קובצי JavaScript עברו `node --check`.
- כל ה־handlers נטענו בהצלחה.
- בתיקיית `api` נמצא קובץ אחד בלבד: `api/index.js`.
- כל נתיבי ה־API ממשיכים לעבור דרך פונקציית Vercel אחת, ולכן המערכת מתאימה לתוכנית Hobby.
- לא נוספו טבלאות, RPC או מיגרציות חדשות.
- גרסת היישום היא `0.6.0` וגרסת מסד הנתונים הנדרשת היא `0.5.0`.
- כל המזהים הקבועים שבהם משתמש `app.js` קיימים ב־`index.html`.
- מבנה ה־CSS וה־SQL עבר בדיקת סוגריים ובלוקים.
- נתיבי Vercel, הנתב המאוחד וה־handlers נמצאו תואמים.

## בדיקות עסקיות שנשמרו

- ארבע נשות צוות במהלך היום.
- שלוש נשות צוות בחלון הסגירה.
- גננת או מובילה בכל מקטע זמן.
- זיהוי חפיפות בין שיבוצים.
- תמיכה במשרות חלקיות ובפיצול יום.
- זיהוי איסור שיבוץ בכיתה.
- הכנת שיבוץ ופרסום במקום זמני/סופי.
- העתקת שבוע קודם.
- הזרמת בקשות בעסקה אחת.
- גננות יכולות ליצור הודעות ומשימות.
- נרמול מספרי טלפון והתחברות ללא מייל.
- הצפנת סיסמאות באמצעות `scrypt`.

## בדיקות אבטחה

- לא נמצא בקבצים ערך של `SUPABASE_SECRET_KEY`.
- המפתח הסודי נשאר בצד השרת בלבד.
- פעולות כתיבה מוגנות בסשן וב־CSRF.
- עובדת רגילה אינה מקבלת הערות ניהוליות או נתוני העסקה פרטיים של אחרות.
- לא נעשה שימוש ב־`eval`, ב־`new Function` או באירועי JavaScript מוטמעים ב־HTML.

## מגבלת הבדיקה

הבדיקות בוצעו מקומית ובדפדפן עם נתוני בדיקה. לא בוצעה מתוך סביבת העבודה הזו פריסה לחשבון Vercel החי שלך ולא נבדקה רשת Supabase החיה. לאחר ההעלאה, האישור הסופי הוא סטטוס `Ready` ב־Vercel ומסך `/health` ירוק.
