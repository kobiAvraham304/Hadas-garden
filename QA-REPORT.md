# דוח בדיקות — מערכת ניהול שיבוצים מעון הדס 0.9.0

## תוצאה

**62 מתוך 62 בדיקות אוטומטיות עברו בהצלחה.**

בנוסף בוצעה בדיקת דפדפן ממוקדת למסך העובדים ולטופס העובד ב־Chromium עם נתוני בדיקה:

| תצוגה | גלישה אופקית | כרטיסי עובדים | גבולות חלון העריכה | שגיאות JavaScript |
|---|---:|---:|---|---:|
| מחשב 1440px | 0 | 7 | 330–1110px | 0 |
| טלפון 390px | 0 | 7 | 8–382px | 0 |
| טלפון 360px | 0 | 7 | 8–352px | 0 |

## ניהול עובדים

- נבדקה רשימת שבעת התפקידים המדויקת.
- נבדק שהרשימה אינה מציגה אזור בחירה ריק.
- נבדקו שישה כרטיסי ימים, ראשון עד שישי.
- נבדקה בחירה במספר ימי חופשה קבועים.
- נבדקו שעות שונות לכל יום עבודה.
- נבדקו כיתה קבועה, רוטציה וללא שיבוץ.
- נבדק שדה מקסימום שעות שבועיות.
- נבדקה הסתרת שדות השיבוץ לאחות ולמזכירה.
- נבדקה תאימות לתפקידי עבר שלא הומרו אוטומטית.

## תקינות ושיבוצים

- נבדקה שגיאה חוסמת בעת חריגה ממקסימום שעות שבועיות.
- נבדקו יום חופשי קבוע ושעות עבודה קבועות לפי יום.
- נבדק שעובד שאינו חלק ממערך השיבוץ אינו יוצר התראת שעות.
- נבדקו חפיפות, תקינת 4/3, גננת או מוביל/ה ואילוצי כיתה.
- נבדק שמנוע המחליפים מסנן עובדים ללא שיבוץ ומתחשב במקסימום שעות.

## הרשאות

- נבדק שגננת, אחות ומזכירה יכולות ליצור הודעות, משימות ואירועי לוח שנה.
- נבדק שסייעת או סייע רגילים אינם מקבלים הרשאת יצירת תוכן אוטומטית.
- נבדק שאחות ומזכירה אינן מקבלות הרשאות ניהול עובדים או שיבוצים.
- נבדקו סשן, CSRF, נעילת התחברות והצפנת סיסמאות `scrypt`.

## מסד נתונים ועדכון

- נבדק שהעדכון `update-v0.9.0.sql` אינו מכיל `DROP TABLE`.
- נבדקו הוספת העמודות והטבלה החדשה ללא מחיקת נתונים.
- נבדקו RLS והרשאות `service_role` לטבלה החדשה.
- נבדקו Trigger של `updated_at` ואותות Realtime.
- נבדק שמספר גרסת מסד הנתונים מתעדכן ל־`0.9.0`.

## קוד, אבטחה ופריסה

- כל קובצי JavaScript עברו בדיקת תחביר וטעינה.
- `npm run check` לא מצא Supabase Secret Key בקבצים.
- נשארה פונקציית Vercel אחת בלבד: `api/index.js`.
- נבדקה התאמה בין נתיבי API, handlers, טבלאות ו־RPC.
- אין `eval`, `new Function` או אירועי JavaScript מוטמעים ב־HTML.
- מספר הגרסה `v0.9.0` הופיע בכל שלוש תצוגות הדפדפן.

צילומי הבדיקה והמדדים נמצאים בתיקיית `qa`. בדיקות הדפדפן בוצעו עם נתוני שרת מדומים; לאחר ההעלאה יש לוודא `Ready` ב־Vercel ומסך `/health` ירוק.
