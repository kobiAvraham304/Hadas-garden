# דוח בדיקות — מערכת ניהול שיבוצים מעון הדס 0.5.0

## תוצאה כוללת

**40 מתוך 40 בדיקות אוטומטיות עברו בהצלחה.**  
בנוסף בוצעה בדיקת דפדפן מלאה עם נתוני דמה במידות מחשב ובמידות טלפון.

## בדיקות תשתית ו־Vercel

- בתיקיית `api` קיים קובץ אחד בלבד: `api/index.js`.
- כל נתיבי השרת מנותבים אל פונקציית Vercel אחת, ולכן הגרסה מתאימה לתוכנית Hobby.
- כל ה־handlers נטענו בהצלחה וכל קובצי JavaScript עברו בדיקת תחביר.
- נבדקה התאמה בין נתיבי `vercel.json`, הנתב המאוחד וכל קובצי הפעולות.
- נבדק מסך `/health` ושהוא מצפה לגרסת מסד נתונים `0.5.0`.
- אין צורך במשתני סביבה חדשים מעבר לשלושת משתני Supabase הקיימים.

## בדיקות שיבוצים ופרסום

- תצוגות שבוע מלא, לפי יום והשיבוץ שלי קיימות ועובדות עם אותו מקור נתונים.
- אין יותר מצבים “זמני/סופי”; קיימים הכנה ופרסום בלבד.
- הוספה, עריכה, מחיקה והעתקת שבוע מתועדות ביומן שינויים לפני הפרסום.
- עובדות רגילות ממשיכות לראות את הגרסה האחרונה שפורסמה עד לפרסום החדש.
- לפני פרסום מתקבלות שגיאות והתרעות תקינה ורשימת השינויים הממתינים.
- העתקת שבוע קודם תומכת בתצוגה מקדימה, החלפה מלאה או מיזוג ללא חפיפות.
- בקשה שאושרה מוזרמת לשיבוץ בעסקת PostgreSQL אחת: עדכון השיבוץ, תיעוד השינוי ועדכון מצב הבקשה מתבצעים יחד.
- חופשה, יום חופשי ומחלה מתועדים כמחיקת שיבוץ ממתינה לפרסום.
- התחלה מאוחרת וסיום מוקדם הופכים את השיבוץ לטיוטה ומתועדים לפני/אחרי.
- החלפה בין עובדות מסומנת כטיוטה, נבדקת מול חפיפות ואילוצי כיתה ומתועדת לשתי העובדות.

## בדיקות תקינה עסקית

- ארבע נשות צוות במהלך היום.
- שלוש נשות צוות בחלון הסגירה.
- גננת או מובילה בכל מקטע זמן.
- זיהוי שיבוצים חופפים.
- תמיכה במשרות חלקיות ובפיצול יום.
- זיהוי איסור שיבוץ בכיתה.
- נרמול מספרי טלפון ישראליים.
- התחברות ללא מייל וללא SMS.
- הצפנת סיסמאות באמצעות `scrypt` ובדיקת הסיסמה הראשונית `hadas`.

## בדיקות ממשק במחשב ובטלפון

המערכת הופעלה בדפדפן Chromium עם נתוני דמה ועם קוד התצוגה האמיתי של הגרסה.

נבדקו:

- מסך ראשי.
- שיבוצים.
- בקשות.
- הודעות.
- משימות.
- לוח שנה.
- ניהול עובדות.
- רוחב מחשב ורוחב טלפון של 390 פיקסלים.
- אין גלישה אופקית של העמוד או המסכים שנבדקו.
- לא נמצאו כפתורים שחופפים זה לזה במובייל.
- ניווט המובייל נשאר קבוע ונגיש בתחתית המסך.
- כל שדות השעה מוצגים בכיוון `LTR` וביישור תקין.
- לוח השנה מציג 42 ימים, שבעה ימים בשורה ואירועים לחיצים.
- לחיצה על אירוע פתחה חלון פרטים מלא.
- לא נרשמו חריגות JavaScript בזמן ההפעלה.

תוצאות המדידה נמצאות ב־`qa/browser-checks.json`, וצילומי המסכים נמצאים בתיקיית `qa`.

## בדיקות אבטחה וקוד

- סריקה שאין בפרויקט ערך של `SUPABASE_SECRET_KEY`.
- המפתח הציבורי בלבד נשלח לדפדפן לצורך Realtime.
- פעולות כתיבה מוגנות בסשן וב־CSRF.
- עובדת רגילה אינה מקבלת הערות ניהוליות, אחוזי משרה או פרטי הרשאה פרטיים של עובדות אחרות.
- גננות יכולות ליצור הודעות ומשימות; סייעות ועובדות אחרות אינן מקבלות הרשאת יצירה אוטומטית.
- מסך המסמכים, נתיב השרת שלו וקוד התצוגה הישן הוסרו.
- לא נעשה שימוש ב־`eval`, ב־`new Function` או באירועי JavaScript מוטמעים ב־HTML.

## מגבלת הבדיקה

הבדיקות בוצעו מקומית ובדפדפן עם נתוני דמה. לא בוצעה על ידי המערכת פריסה לחשבון Vercel שלך ולא הורצה המיגרציה בפרויקט Supabase החי שלך. האישור הסופי לאחר ההעלאה הוא:

1. הרצת `supabase/update-v0.5.0.sql` בהצלחה.
2. סטטוס `Ready` ב־Vercel.
3. מסך `/health` ירוק.
