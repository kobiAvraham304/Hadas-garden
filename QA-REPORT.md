# דוח בדיקות — מערכת מעון הדס 0.4.2

## תוצאה

**28 מתוך 28 בדיקות אוטומטיות עברו בהצלחה.**

## בדיקות שבוצעו

- טעינת כל פונקציות ה־API של Vercel.
- התאמה בין טבלאות ה־API לבין קובץ ה־SQL.
- התאמה בין פונקציות RPC לבין קובץ ה־SQL.
- בדיקת מבנה SQL ו־CSS.
- בדיקת תיקון התקנת גרסה ישנה שבה `hadas_classes` חסרה `updated_at`.
- בדיקה שכל טבלה שמקבלת Trigger של `updated_at` כוללת את העמודה.
- בדיקה שהרשאות `service_role` מוגבלות לאובייקטים של מערכת הדס בלבד.
- בדיקת נרמול מספרי טלפון ישראליים ללא שימוש במייל.
- בדיקת הצפנת ואימות סיסמאות באמצעות scrypt.
- בדיקת התאמת הסיסמה הראשונית `hadas` ל־Hash שב־SQL.
- בדיקות חפיפה, שעות חלקיות, ארבע נשות צוות ושלוש בסגירה.
- בדיקת גננת או מובילה ואילוצי כיתה.
- בדיקת קישורים חתומים למסמכים ב־Supabase Storage.
- בדיקת הרשאות מידע לעובדת רגילה מול מנהלת.
- בדיקת נתיב `/health`, כותרות אבטחה וגרסת Node קבועה.
- בדיקת ממשק סלולר, תצוגות שיבוץ, חיפוש וסינון.
- סריקה שאין מפתח Supabase סודי בקבצים.

פלט הבדיקות המלא נמצא ב־`qa/QA-RUN.txt`.

## מגבלת הבדיקה

לא ניתן היה להריץ את ה־SQL ישירות בפרויקט Supabase החי מתוך סביבת הבדיקה. לכן האישור האחרון יהיה הופעת `Success` ב־SQL Editor ולאחר מכן מסך `/health` ירוק באתר.
