# דוח בדיקות — מערכת ניהול שיבוצים מעון הדס 0.8.0

## תוצאה

**57 מתוך 57 בדיקות אוטומטיות עברו בהצלחה.**

בנוסף הממשק הופעל ב־Chromium עם נתוני בדיקה בשלושה גדלים:

| תצוגה | גלישה אופקית בשיבוצים | גלישה אופקית בעובדים | כרטיסי יום פתוחים בהתחלה | לאחר פתיחת שני ימים | שגיאות JavaScript |
|---|---:|---:|---:|---:|---:|
| מחשב 1440px | 0 | 0 | לא רלוונטי | לא רלוונטי | 0 |
| טלפון 390px | 0 | 0 | 0 | 1 בלבד | 0 |
| טלפון 360px | 0 | 0 | 0 | 1 בלבד | 0 |

## בדיקות תקינה

- שבוע ריק של שלוש כיתות ושישה ימים מפיק 36 שגיאות שרת מקובצות בלבד: 18 מוקדי כוח אדם ו־18 מוקדי אחראי/ת בכיתה, במקום שגיאה לכל מקטע זמן.
- במסך עצמו שני סוגי התקלה מאוחדים ל־18 כרטיסי יום/כיתה.
- לחיצה על שגיאה העבירה לתצוגת יום והדגישה את הכיתה המתאימה בכל שלושת גדלי המסך.
- התראות שעות אישיות נשמרות בנפרד ומובילות לכרטיס העובד.
- נבדקו ארבעה אנשי צוות במהלך היום, שלושה בסגירה וחובת גננת/גנן או מוביל/ה.
- נבדקו חפיפות, אילוצי כיתה, שעות חלקיות והיקף משרה שבועי.

## בדיקות מובייל ויזואליות

- כותרת השבוע נשארה בגובה `68px` מהחלק העליון גם לאחר גלילה עמוקה בטלפונים.
- שישה כרטיסי ימים הוצגו ללא פתיחה אוטומטית.
- פתיחת יום שני סגרה את היום הראשון.
- לא נמצאה גלישה אופקית ברוחב 390px או 360px.
- נבדקו מסכי שיבוצים, לוח שנה וניהול עובדים.
- לחיצה על יום בלוח השנה פתחה טופס אירוע בכל גודל שנבדק.
- מעבר מאוגוסט לספטמבר עדכן את הכותרת והסתיים ללא שגיאת JavaScript.

צילומי הבדיקה ומדדי הדפדפן נמצאים בתיקיית `qa`.

## ייצוא

- נבדקו סטטית תבנית שבועית אחת לרוחב, תבנית חודשית, Web Share, גיבוי להורדה ו־A4 landscape.
- תבנית השבוע נבדקה בפועל ברוחב 1600px ובגובה 766px ונמצאה קריאה וברורה.
- תבנית חודש של חמישה שבועות נמדדה בגודל 1640×2918px, מתחת למגבלת הבטיחות של 3800px.
- נבדקה הגבלת ממדי Canvas עבור ייצוא חודשי במובייל.
- לא ניתן לבדוק שמירה פיזית לגלריית iPhone בסביבת Chromium; במכשיר אמיתי הדפדפן פותח את תפריט השיתוף של iOS, שממנו בוחרים `שמירת תמונה`.

## קוד, אבטחה ופריסה

- כל 28 קובצי JavaScript עברו בדיקת תחביר.
- `npm run check` לא מצא Supabase Secret Key בקבצים.
- בתיקיית `api` קיימת פונקציית Vercel אחת בלבד.
- נבדקה התאמה בין נתבי API, handlers, טבלאות ו־RPC.
- נבדקו סשן, CSRF והצפנת סיסמאות `scrypt`.
- אין `eval`, `new Function` או אירועי JavaScript מוטמעים ב־HTML.

## מגבלה

בדיקות הדפדפן בוצעו עם נתוני שרת מדומים. לאחר ההעלאה יש לוודא `Ready` ב־Vercel ומסך `/health` ירוק.
