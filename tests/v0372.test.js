const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const read=p=>fs.readFileSync(p,'utf8');
test('0.37.2 wires Web Push through API and service worker',()=>{const pkg=JSON.parse(read('package.json'));assert.equal(pkg.version,'0.37.2');assert.equal(pkg.dependencies,undefined);assert.match(read('lib/server.js'),/encryptWebPushPayload/);assert.match(read('api/index.js'),/'push': require\('\.\.\/handlers\/push'\)/);assert.match(read('vercel.json'),/\/api\/push/);assert.match(read('index.html'),/push-v0372\.js\?v=0372/);assert.match(read('push-v0372.js'),/pushManager\.subscribe/);assert.match(read('push-sw.js'),/showNotification/);});
test('0.37.2 keeps private VAPID key server-side',()=>{assert.match(read('handlers/push.js'),/select\('public_key'\)/);assert.doesNotMatch(read('handlers/push.js'),/private_key/);assert.match(read('lib/server.js'),/select\('public_key,private_key,subject'\)/);});
test('0.37.2 pushes popup announcements and automatic operational events',()=>{const a=read('handlers/announcements.js'),s=read('lib/server.js');assert.match(a,/truthy\(body\.send_push\) \|\| truthy\(body\.popup_on_login\)/);assert.match(a,/push: pushRequested/);assert.match(s,/פורסם שיבוץ שבועי חדש/);assert.match(s,/הבקשה שלך אושרה/);assert.match(s,/הבקשה שלך נדחתה/);});
test('0.37.2 provides iPhone and Android setup UX',()=>{const h=read('index.html'),c=read('push-v0372.js');assert.match(h,/התראות בטלפון/);assert.match(h,/שליחת התראה לטלפון/);assert.match(c,/הוספה למסך הבית/);assert.match(c,/Android/);});