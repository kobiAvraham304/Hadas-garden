# גרסה 0.4.3 — התאמה ל־Vercel Hobby

## התקלה שתוקנה

Vercel חסמה את הפריסה משום שבגרסה היו 17 פונקציות Serverless נפרדות, בעוד תוכנית Hobby מאפשרת עד 12.

## התיקון

- כל 17 נתיבי ה־API אוחדו לפונקציה אחת: `api/index.js`.
- קובצי הפעולות הועברו לתיקיית `handlers`, ולכן אינם נספרים כפונקציות Vercel נפרדות.
- נוספו ניתובים שקופים, כך שהאתר ממשיך להשתמש בכתובות הקיימות כמו `/api/shifts` ו־`/api/employees`.
- לא בוטל אף פיצ'ר ולא שונתה צורת העבודה של המשתמשות.

## בסיס נתונים

אין שינוי בסכמת Supabase. גרסת הסכמה נשארת 0.4.1, ולכן מי שכבר הריץ אותה בהצלחה לא צריך להריץ SQL מחדש.
