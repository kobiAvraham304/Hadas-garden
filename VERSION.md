# הערות גרסה — מערכת מעון הדס 0.4.1

## תיקון התקנת Supabase

- תוקנה השגיאה `record "new" has no field "updated_at"`.
- מקור השגיאה היה תאימות לגרסה הישנה שבה `hadas_classes` נוצרה ללא `updated_at`.
- קובץ ה־SQL הוא כעת מתקין נקי: הוא מסיר רק אובייקטים עם הקידומת `hadas_` ומקים אותם מחדש.
- הוסרו Triggers ופונקציות ישנים לפני יצירת המבנה החדש.
- נוספה בדיקה אוטומטית שכל טבלה שמקבלת Trigger של `updated_at` אכן כוללת את העמודה.
- הרשאות `service_role` ניתנות רק לטבלאות ולרצפים של מערכת הדס, ולא לכל סכמת `public`.
- גרסת מסד הנתונים, ה־API והאתר עודכנה ל־0.4.1.

## אין צורך בגרסאות SQL קודמות

אין להריץ את SQL של 0.3.0 או 0.4.0. יש להריץ רק את `supabase/schema.sql` של 0.4.1.
