# העלאת גרסה 0.9.0 ל־Vercel

## 1. עדכון Supabase

ב־SQL Editor הרץ פעם אחת את:

```text
supabase/update-v0.9.0.sql
```

אין להריץ את `schema.sql` על המערכת הפעילה.

## 2. העלאת הקוד

1. החלף ב־GitHub את כל קובצי גרסה 0.8.0 בקובצי 0.9.0.
2. ודא שבתיקיית `api` נשאר רק `api/index.js`.
3. בצע Commit ו־Push.
4. המתן לסטטוס `Ready` ב־Vercel.
5. פתח `/health` וודא שכל השורות ירוקות וגרסת מסד הנתונים היא `0.9.0`.
6. באייפון סגור את לשונית האתר ופתח אותה מחדש; במחשב בצע רענון מלא.

אין צורך לשנות את משתני הסביבה ואין צורך בתוכנית Vercel Pro.
