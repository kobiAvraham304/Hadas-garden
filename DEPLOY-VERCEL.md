# העלאת גרסה 0.10.0 ל־Vercel

## 1. עדכון Supabase

המערכת הפעילה נמצאת בגרסה 0.9.0. ב־SQL Editor הרץ פעם אחת את:

```text
supabase/update-v0.10.0.sql
```

אין להריץ את `schema.sql` על המערכת הפעילה.

העדכון מוסיף טבלת עדכונים אישיים, טווחי תאריכים לבקשות ודלי פרטי לאישורי מחלה. הוא אינו מוחק נתונים קיימים.

## 2. העלאת הקוד

1. החלף ב־GitHub את כל קובצי גרסה 0.9.0 בקובצי 0.10.0.
2. ודא שבתיקיית `api` נשאר רק `api/index.js`.
3. בצע Commit ו־Push.
4. המתן לסטטוס `Ready` ב־Vercel.
5. פתח `/health` וודא שכל השורות ירוקות וגרסת מסד הנתונים היא `0.10.0`.
6. באייפון סגור את לשונית האתר ופתח אותה מחדש. במחשב בצע רענון מלא.

אין צורך לשנות משתני סביבה ואין צורך בתוכנית Vercel Pro.
