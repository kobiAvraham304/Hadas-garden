# פריסת 0.30.0

1. ודא שכל מיגרציות Supabase של גרסה 0.30.0 הוחלו בפרויקט הדס.
2. העלה את קובצי הגרסה לענף `main`.
3. אין שינוי במשתני Vercel אלא אם צוין אחרת בגרסה.
4. המתן ל־`Ready` בפריסת Production.
5. בדוק `/api/config` ו־`/api/health` ושניהם צריכים להחזיר גרסה `0.30.0`.
6. ודא ש־`/patch-v025.js` מגיש את bootstrap של 0.30 ולא את ממשק 0.25 הישן.
7. בצע בדיקת עשן למסכי השיבוצים, התפעול היומי, ההתראות ולוח השנה ובצע רענון קשיח אחד במכשיר נייד קיים.
